query GetHome {
  homeSingletons(first: 1) {
    nodes {
      id

      # Hero (asset + caption)
      heroImage
      heroImageCaption

      # Reels: selector de proyectos (IDs); el copy va en messages/{locale}.json
      reelsSelected         # repeater → JSON string
    }
  }
}
