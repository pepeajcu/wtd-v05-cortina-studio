---
date: 2026-05-16
session: 3
tags: [fase-5, qa, iconmap, readme, replicacion, jetengine]
summary: Fase 5: validado iconMap (fix mismatch wp-config), preparado recursos/ para export JetEngine, reescrito README con playbook de replicacion
---

# Sesion 2026-05-16 #3 — Fase 5 puntos 3, 4 (parcial) y 5

## Contexto previo

Fase 4 ya cerrada en codigo (commit `3cc636a` "Avance hasta Fase 3.6 del Plan" cubrio tambien Fase 4). Solo quedaba `[~]` el bullet de validacion end-to-end del webhook, diferido a Fase 5 porque requiere `next start` + tunel o deploy. Usuario decide no desplegar todavia (queda desarrollo pendiente) y pide ejecutar los puntos de Fase 5 que SI se pueden hacer sin sitio publicado: 3 (iconMap), 4 (export JetEngine) y 5 (README playbook).

## Que se hizo

- Validacion iconMap (punto 3): compare contenido real del CMS (`HOME-SINGLETON-CONTENIDO.md`) contra `lib/iconMap.tsx`. Los 8 valores (`Thermometer`/`EyeOff`/`VolumeX`/`Sparkles` para `problems_cards` y `whatsapp`/`mappin`/`palette`/`wrench` para `process_steps`) resuelven contra el REGISTRY. Fallback `HelpCircle` con warning solo en dev.
- Correccion `wp-config.json.iconMap.process`: declaraba valores PascalCase (`WhatsApp`/`MapPin`/`Palette`/`Wrench`) que no existen ni en el CMS ni en el REGISTRY. Reemplazados por los slugs lowercase reales (`whatsapp`/`mappin`/`palette`/`wrench`) para que el JSON sirva como documentacion verdadera.
- Punto 4 (export JetEngine): creado `recursos/README.md` con inventario esperado, procedimiento manual paso a paso (JetEngine → Tools → Export marcando Post Types `home` y `proyecto`, Meta Boxes asociados, Options Page `general`), nombre final esperado (`jetengine-cortinastudio.json`) e instrucciones de import en cliente nuevo. La accion fisica del export queda como TODO del usuario en el panel WP.
- Punto 5 (README playbook): reescrito `README.md` completo, reemplazando el boilerplate de create-next-app. Incluye reglas no negociables, setup, tabla de variables de entorno, scripts, estructura del repo, playbook por fase (Frontend / Backend WP / Conexion / Validacion) referenciando la skill `replicacion-cliente`, comandos de verificacion y estado actual.
- Actualizado `docs/specs/001-plan-inicial/task.md`: bullets de Fase 5 marcados — 3 como `[x]` con nota de la correccion, 4 como `[~]` con referencia a `recursos/README.md`, 5 como `[x]`. Bullets 1 (revalidacion CPTs) y 6 (segundo cliente) anotados como bloqueados por deploy/cliente real.

## Decisiones tomadas

- **Corregir `wp-config.json.iconMap.process` antes que el comentario engañoso en `lib/iconMap.tsx:36`.** El comentario dice "Mapping autoritativo: `wp-config.json.iconMap`" pero el codigo no lee el JSON. Opcion A elegida: alinear el JSON con la verdad para que sirva de documentacion util (los slugs lowercase reales del CMS). Razon: borrar `iconMap` del JSON (opcion B) perdia la documentacion; implementar lookup real (opcion C) era sobre-engineering para 8 valores estables.
- **Punto 4 marcado `[~]` y no `[x]`.** La accion humana del export en el panel WP no se ejecuto; solo se preparo la infraestructura documental. Diferenciar `[x]` (hecho) vs `[~]` (preparado/parcial) preserva la honestidad del checklist.
- **No avanzar bullets 1, 2 y 6 de Fase 5 ni las verificaciones globales.** Usuario pidio explicitamente parar ahi hasta que toque deploy. Bullet 1 (revalidacion CPTs) requiere tunel/deploy, bullet 6 (segundo cliente) requiere cliente real; quedan en hold sin pretender ejecutarlos a medias.
- **README.md reescrito completo (no parcheado).** El original era boilerplate de create-next-app sin contenido util. Reescritura limpia permite alinearlo con la skill `replicacion-cliente` y con la realidad del repo.

## Pendientes / siguientes pasos

- Usuario debe ejecutar manualmente el export en WP y guardar el JSON como `recursos/jetengine-cortinastudio.json`. Tras eso, marcar el bullet 4 como `[x]`.
- Cuando se haga deploy: bullets 1 y 2 de Fase 5 (revalidacion en todos los CPTs + fallback con WP caido) y cierre del `[~]` final de Fase 4 (webhook end-to-end).
- Bullet 6 (segundo cliente conectandose a otro WP) queda en hold hasta que aparezca un cliente real.
- Verificaciones tecnicas globales (`task.md:192-196`): no se revisaron en esta sesion; deben hacerse antes del cierre formal de Fase 5.

## Archivos tocados

**Creados:**
- `recursos/README.md`

**Modificados:**
- `wp-config.json` (solo bloque `iconMap.process`)
- `README.md` (reescritura completa)
- `docs/specs/001-plan-inicial/task.md` (bullets de Fase 5)

**No tocados (verificacion explicita):**
- `lib/iconMap.tsx` — el REGISTRY es correcto; solo es engañoso el comentario sobre `wp-config.json.iconMap`. No se modifico para no inflar el alcance; la correccion se hizo del lado del JSON.
- `lib/wordpress/*`, `app/[locale]/*`, `components/sections/*` — codigo de fabrica/cliente no requiere cambios para los puntos 3-5 de Fase 5.
